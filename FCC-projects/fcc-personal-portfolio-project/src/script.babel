// for later use
//var showMessage = function (target, message, index, interval) {   
//  if (index < message.length) {
   // document.getElementById(target).innerHTML += message[index++];
//    setTimeout(function () { showMessage(target, message, index, interval); }, interval);
//  }
//}